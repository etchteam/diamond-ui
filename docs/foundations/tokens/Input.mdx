import { Meta, Source, Canvas } from '@storybook/blocks';
import { Tokens } from '@etchteam/storybook-addon-design-token-tables';

<Meta title="Foundations/Tokens/Input" />

# Input

These tokens handle the styling of form inputs, such as [dmd-input](?path=/docs/control-input--docs) and [dmd-radio-checkbox](?path=/docs/control-radiocheckbox--docs).
They include tokens for states, such as disabled, hover, valid and invalid.

<Tokens collection="dmd-input" />
